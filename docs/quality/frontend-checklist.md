# Frontend Checklist

- 페이지는 조합만 하고 상태/행동은 feature로 보낸다.
- 공용 UI와 도메인 UI를 섞지 않는다.
- 같은 도메인 규칙 계산이 여러 파일에 중복되면 selector/helper로 통일한다.
- 리팩터링 후 문서와 타입 정의를 같이 갱신한다.
- 전역 레이아웃에서 공통 상태를 조합하면 소비 지점은 직접 재조합하지 않는다.
